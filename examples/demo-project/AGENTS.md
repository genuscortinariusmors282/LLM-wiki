# Acme Widget Factory Agent Rules

这仓库默认走 `wiki-first`，不是 `chat-first`。

1. 先读 `docs/wiki/index.md`
2. 再读 `docs/wiki/current-status.md`
3. 再读 `docs/wiki/log.md`

默认范式：

- `compile-first`
- `writeback` 必做
- 先 `wiki`，不先上重 `RAG`
- `Idea / Intent` 优先于 `Code`
